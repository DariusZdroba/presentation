import React from 'react'

const NotFound = () => {
  return (

    <section className="info-section">
        <h1 className='section-header'>
            The page you are looking for is not found / doesn't exist!
        </h1>
        </section>
)
}

export default NotFound